#!/bin/bash
# Stop hook: Advisory check for React component completeness
# Checks if components have loading/empty/error states
# Exit 0 always (advisory only, does not block)

# Advisory — future enhancement: parse AST for state handling patterns
exit 0
