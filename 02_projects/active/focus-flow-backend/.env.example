# Server Configuration
PORT=3001
NODE_ENV=development

# Vault Path
VAULT_PATH=/srv/focus-flow

# OpenClaw Configuration
# SECURITY: Gateway MUST run on localhost only. Never expose to public internet.
OPENCLAW_GATEWAY_URL=http://localhost:18789
# OPENCLAW_AUTH_TOKEN is loaded from /srv/focus-flow/07_system/secrets/.openclaw.env
# Generate with: openssl rand -hex 32

# Security Settings
OPENCLAW_MAX_TOKENS=4000
OPENCLAW_TIMEOUT_MS=60000
OPENCLAW_ENABLE_AUDIT_LOG=true

# CRITICAL SECURITY NOTES:
# 1. Gateway MUST bind to 127.0.0.1 (localhost), NOT 0.0.0.0
# 2. Auth token MUST be generated randomly (openssl rand -hex 32)
# 3. Store auth token in /srv/focus-flow/07_system/secrets/.openclaw.env
# 4. Enable firewall: sudo ufw deny 18789/tcp
# 5. Never commit .env files to version control

# Legacy - No longer used
# ANTHROPIC_API_KEY is no longer needed (using OpenClaw with Claude subscription)

# Telegram Bot (optional)
TELEGRAM_BOT_TOKEN=your-bot-token-here

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
